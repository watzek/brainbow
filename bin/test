#!/usr/bin/env bash
# Launch a dev server at http://localhost:8080/ and run tests on it

trap stop_server SIGINT SIGTERM

function stop_server {
    printf "\nShutting down gracefully...\n"
    docker-compose stop
    exit 0
}

docker-compose up -d
sleep 5
casperjs test test/*.js
docker-compose stop
